# Orthodox Canonical class template

This programm creates a .hpp and .cpp file for the given name (as first parameter)
It fills those file with Basic Constructor/Destructor/Copy constructor and copy assignment constructor.
The class is named after your input.
For best use add an alias to the execuable
